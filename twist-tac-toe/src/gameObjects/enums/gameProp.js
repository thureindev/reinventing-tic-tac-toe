/**
 * Enum for the properties of the game object.
 * @readonly
 * @enum {string} - 
 */
const GameProp = Object.freeze({
    SIZE: 'size',
    WIN_LENGTH: 'win-length',
    IS_LIMITED_PIECES: 'is-limited-pieces',
    NUM_PIECES: 'num-pieces',
    NONE: null,
});

export default GameProp;
